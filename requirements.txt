# Dipendenze runtime dello screener (job GitHub Actions). Plotly NON serve a
# runtime: build_page ingloba assets/plotly.min.js (committato nel repo).
pandas>=2.2,<3
numpy>=1.26
pyarrow>=15
matplotlib>=3.8
requests>=2.31
PyYAML>=6.0
